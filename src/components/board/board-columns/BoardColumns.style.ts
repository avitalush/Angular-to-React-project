import type { SxProps } from "@mui/material";

const container: SxProps = {
  display: "flex",
  gap: 2,
  pb: 2,
};

const columnWrapper: SxProps = {
  flex: 1,
  minWidth: 280,
};

export default {
  container,
  columnWrapper,
};
