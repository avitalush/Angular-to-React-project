import type { SxProps } from "@mui/material";

const root: SxProps = {
  backgroundColor: "#f5f5f5",
  minHeight: "100vh",
  py: 3,
};

const dragOverlayCard: SxProps = {
  transform: "rotate(5deg)",
};

export default {
  root,
  dragOverlayCard,
};
